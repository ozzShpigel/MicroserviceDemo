apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-app
spec:
  selector:
    app.kubernetes.io/name: {{ .Release.Name }}-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: {{ .Values.service.type}}